AGENT_NAME="HRRecruitingAssistant"
AGENT_PORT=8080

# URLs for the platform services (adjust if deployed elsewhere)
A2A_REGISTRY_URL=http://localhost:8000
# Direct URLs might be used if registry is very simple or bypassed in dev
# AUTH_AGENT_URL=http://localhost:8001/a2a
# WEBSERVICE_AGENT_URL=http://localhost:8002/a2a
# DBSERVICE_AGENT_URL=http://localhost:8003/a2a

# Add any necessary API keys or credentials here (use Secret Manager in prod)
# SOME_API_KEY=your_key_here